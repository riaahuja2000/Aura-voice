// Backward-compatible import name used by the auth screens.
// The visual itself is now the canonical Aura Voice brand system.
export { AuraVoiceWordmark as AureliaWordmark } from "@/src/components/AuraVoiceBrand";
